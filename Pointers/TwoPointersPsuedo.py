function fn(arr1, arr2):
    i = j = 0
    while i < arr1.length AND j < arr2.length:
        # Do some logic here for the problem at hand
        # Do some logic here to decide
        1. i++
        2. j++
        3. both i/j++

    # Step 4: make sure both iters are exhausted
    # NOTE: only one of these loops would run
    while i < arr1.length:
        # do logic for loop one
        i++

    while j < arr2.length:
        # arr2 ops
        j++
